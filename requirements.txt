# ═══════════════════════════════════════════════════════════════════
# Cardinal_Multi — зависимости
# Установка:
#   pip install -r requirements.txt
#   playwright install chromium   # только если используешь Lolzteam без API-токена
# ═══════════════════════════════════════════════════════════════════

# ── Ядро / логирование ──────────────────────────────────────────────
loguru>=0.7.0,<1.0
rich>=13.0.0,<14.0

# ── Конфигурация (.env) ─────────────────────────────────────────────
pydantic>=2.5.0,<3.0
pydantic-settings>=2.0.0,<3.0

# ── Шифрование (golden_key / telegram_token в БД) ───────────────────
cryptography>=41.0.0,<43.0

# ── База данных (SQLAlchemy async + SQLite) ─────────────────────────
SQLAlchemy>=2.0.0,<3.0
aiosqlite>=0.19.0,<1.0
alembic>=1.12.0,<2.0

# ── HTTP-клиент ─────────────────────────────────────────────────────
aiohttp>=3.9.5,<4.0

# ── Планировщик задач ───────────────────────────────────────────────
apscheduler>=3.10.4,<4.0

# ── Telegram (два клиента используются одновременно) ────────────────
# setup.py  → синхронная валидация через telebot
pyTelegramBotAPI>=4.14.0,<5.0
# bot/*     → async runtime через aiogram 3.x
aiogram>=3.4.0,<4.0

# ── Lolzteam (Playwright — fallback без API-токена) ──────────────────
playwright>=1.40.0,<2.0

# ── Кэш ─────────────────────────────────────────────────────────────
cachetools>=5.3.0,<6.0